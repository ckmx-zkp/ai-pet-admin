# 06 — 协作看板（ai-pet-admin）

> 用途：跟踪本仓开发任务的流转状态 + 跨仓依赖。  
> 约定：改状态直接编辑本文件并提交；任务粒度对齐 `docs/04-开发任务清单.md`。
> 跨仓状态以 `work_dashboard/AI-Pet协作看板.md` 为准。

## 看板

### 🔲 待办（Backlog）

| 任务 | 说明 | 依赖 |
|------|------|------|
| 导出入口 | 后端仍为 501，不得画成可用 | backend E8 `/export` |
| 人设问卷/预览 | 不得在前端算 MBTI | backend E2.1 |
| 记忆画像页 | 无管理端画像接口 | backend E6.1 |
| 每日运势/八字运营页 | 契约 2026-08-18 才登记；admin 是否可见未拍板 | backend B8 / 产品拍板 |

### 🔧 进行中

（空）

### 🤝 待联调（等后端/真机）

| 事项 | 阻塞方 | 说明 |
|------|--------|------|
| 真实对话历史/外设/候选记忆填满页面 | xiaozhi 真机 E2E | 页面已接接口；旁路五类与 Memory MCP 真机调用仍待验收 |
| worker 持续产出摘要与成长建议 | ai-pet-backend worker | 已有过真实 `daily_summary`；需持续会话才能验收卡片 |
| Caddy + 域名 HTTPS | 运维 O1 | 当前公网 HTTP `:8080` 仅适合内测 |

## 后端接口可用状态（截至 2026-08-18）

线上入口：`http://39.107.143.71:8080`（Nginx 同源反代 backend `8010`）。

| 接口 | 状态 | 说明 |
|------|------|------|
| `GET /healthz` | ✅ 可用 | 探活 |
| `POST /api/auth/register` `login` `GET /me` | ✅ 已验证 | login_name + 密码 |
| `GET /api/admin/devices` `lookup` `/{id}` | ✅ 已上线 | 资产分页/精确查询/详情 |
| `POST /api/admin/devices/{id}/binding-id/rotate` | ✅ 已上线 | 不改用户归属 |
| `GET/PUT /api/admin/devices/{id}/persona` | ✅ 已上线 | 含 dossier；未认领 409 |
| `GET /api/admin/devices/{id}/messages` | ✅ 已上线 | 只读 + 时间窗分页 |
| `GET/POST /api/admin/devices/{id}/memories*` | ✅ 已上线 | 列表 + candidate 审核 |
| `GET /api/admin/devices/{id}/analyses` | ✅ 已上线 | daily_summary / persona_growth |
| `GET /api/admin/devices/{id}/peripheral` | ✅ 已上线 | 无快照 404 |
| `/api/admin/kb/*` | ✅ 已上线 | 草稿/发布/反馈审核 |
| `POST /api/devices/bind` | ⛔ 管理台禁用 | 仅用户端；admin 调 403 |
| `POST /api/devices/{id}/export` | 🚧 501 | 不得开发真实导出 |
| `/api/internal/*` | ⛔ 前端不用 | 服务间 token |

### ✅ 已完成

| 事项 | 日期 |
|------|------|
| 项目文档 00–05 | 2026-07-26 |
| 仓库初始化并推送 GitHub | 2026-07-26 |
| A1/A2/A3 壳子 + 登录联调 + ECS:8080 部署 | 2026-08-01 |
| B1 设备管理闭环；B1.1 撤除用户 bind | 2026-08-02 |
| 管理端资产/人设/历史/外设/分析接入 | 2026-08-02 |
| C1/C2 KB 运营、B4 记忆审核 | 2026-08-02 |
| D1/D2/D3/D5：分析卡片、资产真分页、统一空态错误、dossier 下次会话提示；文档回写 | 2026-08-18 |

## 验收对照（对齐产品 §8.2）

- [x] 绑定设备（用户端认领；管理台只读资产）
- [x] 设双鱼 + MBTI
- [x] 看得到历史与记忆
- [x] admin 能发布 water 包

## 进度日志

| 日期 | 仓库 | 事项 |
|------|------|------|
| 2026-08-02 | ai-pet-admin | B1 设备管理闭环完成：对接 devices 五端点，新增绑定、详情、改名、解绑，修正在线状态映射；构建并部署到 ECS:8080。 |
| 2026-08-02 | ai-pet-admin | B1.1 完成：E1.1 上线后撤除管理台对用户 `/devices/bind` 的调用，明确设备认领仅由用户端以 `binding_id` 完成；构建并部署 ECS:8080。 |
| 2026-08-02 | ai-pet-admin | B1.1 管理端设备资产接口已接入：支持全量/模糊搜索与按 MAC/SN 精确查询、查看和轮换 `binding_id`；设备详情接入管理员人设读写、脱敏历史、外设状态和分析只读面板。 |
| 2026-08-02 | ai-pet-admin | C1/C2 知识库运营、B4 记忆审核已接入并部署。 |
| 2026-08-18 | ai-pet-admin | D1 分析改为 daily_summary / persona_growth 卡片；D2 资产列表 20 条 offset 分页；D3 统一 403/404/网络空态与重试；D5 人设保存提示下次会话生效并展示 kb_version。docs/02/03/04/06 回写。 |

## 使用约定

1. 开始某任务 → 把行从「待办」挪到「进行中」并 @ 自己；
2. 代码合入但需后端联调 → 挪到「待联调」；
3. 联调通过 → 挪到「已完成」并写日期；
4. 跨仓阻塞事项优先登记在「待联调」，每周过一遍。
